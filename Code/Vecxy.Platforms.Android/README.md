# Vecxy.Platforms.Android

The Android platform package owns the Silk.NET Activity, touch forwarding,
asset extraction, manifest fragments and Android MSBuild defaults. Games select
it through `-p:VecxyPlatform=Android`; they do not define an Activity or a
second project.
